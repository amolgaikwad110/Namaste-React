import SearchBar from "./SearchBar";
import RestContainer from "./RestContainer";

const Body = () => {
    return (
        <div className="body">
            <SearchBar />
            <RestContainer />
        </div>
    );
};

export default Body;